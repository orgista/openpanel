<?xml version="1.0" encoding="utf-8"?>
<!-- Cold-start window background: plain dark fill only. No logo mark, so the
     boot hands off seamlessly into the in-app wordmark launch animation. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/openpanel_launch_background" />
</layer-list>
